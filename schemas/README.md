# Order JSON Schema

Draft 2020-12 schema for the market engine `Order` structure.

## Validate examples

```bash
python schemas/validate.py
```

Optional dependency: `pip install jsonschema` for full validation.
